<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:auth="clr-namespace:WanderersDiary.Client.ViewModels.Auth"
             x:Class="WanderersDiary.Client.Views.Auth.EmailConfirmationPage"
             x:DataType="auth:EmailConfirmationPageVM"
             BackgroundColor="{DynamicResource PageBackgroundColor}">
    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center" VerticalOptions="Center" Padding="15" Spacing="15">
            <Label Style="{StaticResource DisplayTitleLabelStyle}" Text="Отлично" Margin="0,0,0,20"/>
            <Label Style="{StaticResource BaseTextLabelStyle}" Text="Эмейл подтверждён!" FontSize="18"/>
            <Button Style="{StaticResource BaseButtonStyle}" Text="{Binding Resources[Common_Close]}" Margin="0,15"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>