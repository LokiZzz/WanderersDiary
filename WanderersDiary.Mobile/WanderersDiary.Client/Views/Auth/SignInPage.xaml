<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:auth="clr-namespace:WanderersDiary.Client.ViewModels.Auth"
             x:Class="WanderersDiary.Client.Views.Auth.SignInPage"
             x:DataType="auth:SignInPageVM"
             BackgroundColor="{DynamicResource PageBackgroundColor}">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <StackLayout Grid.Row="0" VerticalOptions="Center">
                <Entry Text="{Binding Login}" />
                <Entry Text="{Binding Password}" IsPassword="True"/>
            </StackLayout>
            
            <Button Grid.Row="1" Text="{Binding Resources[SignInPage_SignIn]}" Command="{Binding SignInCommand}"/>


        </Grid>
    </ContentPage.Content>
</ContentPage>